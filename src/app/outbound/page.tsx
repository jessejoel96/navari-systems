import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Outbound — Navari Systems",
  robots: { index: false, follow: false },
};

export { default } from "./page.client";
